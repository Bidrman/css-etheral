import React from 'react'

const Logo = ({ size }) => {
    return (
        <div className={`logo ${size}`}>
            <h4 className='logo__heading'>Etheral</h4>
        </div>
    )
}

export default Logo
